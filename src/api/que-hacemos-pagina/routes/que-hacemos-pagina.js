'use strict';

/**
 * que-hacemos-pagina router.
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::que-hacemos-pagina.que-hacemos-pagina');
