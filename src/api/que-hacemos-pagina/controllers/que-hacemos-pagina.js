'use strict';

/**
 *  que-hacemos-pagina controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::que-hacemos-pagina.que-hacemos-pagina');
