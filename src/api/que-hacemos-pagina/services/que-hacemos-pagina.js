'use strict';

/**
 * que-hacemos-pagina service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::que-hacemos-pagina.que-hacemos-pagina');
